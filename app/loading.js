import LoadingScreen from '@/components/LoadingScreen';

// Pendant le chargement d'une page publique (accueil, connexion, facture du client…)
export default function Loading() {
  return <LoadingScreen />;
}
